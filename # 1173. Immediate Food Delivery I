# 1173. Immediate Food Delivery I

select round(sum(type)/count(type)*100,2) as immediate_percentage from
(select Case
    when order_date = customer_pref_delivery_date then 1
    ELSE 0
    end as type
from Delivery) sub_t;
