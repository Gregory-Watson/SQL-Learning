# 1113. Reported Posts
select extra as report_reason, count(extra) as report_count from 
(select * from Actions 
where action = 'report'
and action_date = '2019-07-04'
group by post_id, extra) oh
group by report_reason
;
